{
  "buildCommand": "npm run build && mkdir -p public && echo 'API sem site estatico; tudo cai em /api.' > public/index.html",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
